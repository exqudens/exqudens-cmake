cmake_minimum_required(VERSION 3.21 FATAL_ERROR)
get_filename_component(RAW_NAME "${CMAKE_CURRENT_SOURCE_DIR}" NAME)
string(REPLACE " " "_" NAME "${RAW_NAME}")
file(READ "${CMAKE_CURRENT_SOURCE_DIR}/version.txt" RAW_VERSION)
string(STRIP "${RAW_VERSION}" VERSION)
project("${NAME}" VERSION "${VERSION}" LANGUAGES "NONE")

message("project: '${PROJECT_NAME}-${PROJECT_VERSION}'")

include("src/main/cmake/util.cmake")

set_if_not_defined(MESSAGE_MODE "STATUS")

message("${MESSAGE_MODE}" "--- info-1 ---")
message("${MESSAGE_MODE}" "CMAKE_COMMAND: '${CMAKE_COMMAND}'")
message("${MESSAGE_MODE}" "CMAKE_VERSION: '${CMAKE_VERSION}'")
message("${MESSAGE_MODE}" "ENV{PATH}: '$ENV{PATH}'")
message("${MESSAGE_MODE}" "CMAKE_INSTALL_PREFIX: '${CMAKE_INSTALL_PREFIX}'")
message("${MESSAGE_MODE}" "CMAKE_AR: '${CMAKE_AR}'")
message("${MESSAGE_MODE}" "CMAKE_CXX_COMPILER: '${CMAKE_CXX_COMPILER}'")
message("${MESSAGE_MODE}" "CMAKE_C_COMPILER: '${CMAKE_C_COMPILER}'")
message("${MESSAGE_MODE}" "CMAKE_LINKER: '${CMAKE_LINKER}'")
message("${MESSAGE_MODE}" "CMAKE_MT: '${CMAKE_MT}'")
message("${MESSAGE_MODE}" "CMAKE_RC_COMPILER: '${CMAKE_RC_COMPILER}'")
message("${MESSAGE_MODE}" "ENV{INCLUDE}: '$ENV{INCLUDE}'")
message("${MESSAGE_MODE}" "ENV{LIBPATH}: '$ENV{LIBPATH}'")
message("${MESSAGE_MODE}" "ENV{LIB}: '$ENV{LIB}'")

if("${CMAKE_INSTALL_PREFIX_INITIALIZED_TO_DEFAULT}")
    set(CMAKE_INSTALL_PREFIX "${PROJECT_BINARY_DIR}/cmake-install" CACHE PATH "..." FORCE)
endif()

if("Windows" STREQUAL "${CMAKE_HOST_SYSTEM_NAME}" AND "msvc" STREQUAL "${COMPILER_NAME}")
    if(
        "" STREQUAL "${${PROJECT_NAME}_MSVC_INCLUDE}"
        OR "" STREQUAL "${${PROJECT_NAME}_MSVC_LIBPATH}"
        OR "" STREQUAL "${${PROJECT_NAME}_MSVC_LIB}"
        OR "" STREQUAL "${${PROJECT_NAME}_MSVC_PATH}"
    )
        set_msvc_env("${PROJECT_NAME}" "" "${COMPILER_VERSION}" "${COMPILER_ARCH}" "${TARGET_ARCH}")
        set("${PROJECT_NAME}_MSVC_INCLUDE" "${${PROJECT_NAME}_MSVC_INCLUDE}" CACHE STRING "")
        set("${PROJECT_NAME}_MSVC_LIBPATH" "${${PROJECT_NAME}_MSVC_LIBPATH}" CACHE STRING "")
        set("${PROJECT_NAME}_MSVC_LIB" "${${PROJECT_NAME}_MSVC_LIB}" CACHE STRING "")
        set("${PROJECT_NAME}_MSVC_PATH" "${${PROJECT_NAME}_MSVC_PATH}" CACHE STRING "")
    endif()
    set("ENV{INCLUDE}" "${${PROJECT_NAME}_MSVC_INCLUDE}")
    set("ENV{LIBPATH}" "${${PROJECT_NAME}_MSVC_LIBPATH}")
    set("ENV{LIB}" "${${PROJECT_NAME}_MSVC_LIB}")
    set("ENV{PATH}" "${${PROJECT_NAME}_MSVC_PATH}\;$ENV{PATH}")
endif()

find_program(TMP_CMAKE_AR "${CMAKE_AR}" NO_CACHE)
find_program(TMP_CMAKE_CXX_COMPILER "${CMAKE_CXX_COMPILER}" NO_CACHE)
find_program(TMP_CMAKE_C_COMPILER "${CMAKE_C_COMPILER}" NO_CACHE)
find_program(TMP_CMAKE_LINKER "${CMAKE_LINKER}" NO_CACHE)
find_program(TMP_CMAKE_MT "${CMAKE_MT}" NO_CACHE)
find_program(TMP_CMAKE_RC_COMPILER "${CMAKE_RC_COMPILER}" NO_CACHE)

set("CMAKE_AR" "${TMP_CMAKE_AR}" CACHE FILEPATH "" FORCE)
set("CMAKE_CXX_COMPILER" "${TMP_CMAKE_CXX_COMPILER}" CACHE FILEPATH "" FORCE)
set("CMAKE_C_COMPILER" "${TMP_CMAKE_C_COMPILER}" CACHE FILEPATH "" FORCE)
set("CMAKE_LINKER" "${TMP_CMAKE_LINKER}" CACHE FILEPATH "" FORCE)
set("CMAKE_MT" "${TMP_CMAKE_MT}" CACHE FILEPATH "" FORCE)
set("CMAKE_RC_COMPILER" "${TMP_CMAKE_RC_COMPILER}" CACHE FILEPATH "" FORCE)

enable_language("C")
enable_language("CXX")

message("${MESSAGE_MODE}" "--- info-2 ---")
message("${MESSAGE_MODE}" "CMAKE_COMMAND: '${CMAKE_COMMAND}'")
message("${MESSAGE_MODE}" "CMAKE_VERSION: '${CMAKE_VERSION}'")
message("${MESSAGE_MODE}" "ENV{PATH}: '$ENV{PATH}'")
message("${MESSAGE_MODE}" "CMAKE_INSTALL_PREFIX: '${CMAKE_INSTALL_PREFIX}'")
message("${MESSAGE_MODE}" "CMAKE_AR: '${CMAKE_AR}'")
message("${MESSAGE_MODE}" "CMAKE_CXX_COMPILER: '${CMAKE_CXX_COMPILER}'")
message("${MESSAGE_MODE}" "CMAKE_C_COMPILER: '${CMAKE_C_COMPILER}'")
message("${MESSAGE_MODE}" "CMAKE_LINKER: '${CMAKE_LINKER}'")
message("${MESSAGE_MODE}" "CMAKE_MT: '${CMAKE_MT}'")
message("${MESSAGE_MODE}" "CMAKE_RC_COMPILER: '${CMAKE_RC_COMPILER}'")
message("${MESSAGE_MODE}" "ENV{INCLUDE}: '$ENV{INCLUDE}'")
message("${MESSAGE_MODE}" "ENV{LIBPATH}: '$ENV{LIBPATH}'")
message("${MESSAGE_MODE}" "ENV{LIB}: '$ENV{LIB}'")

#message("${MESSAGE_MODE}" "CMAKE_CXX_IMPLICIT_INCLUDE_DIRECTORIES: '${CMAKE_CXX_IMPLICIT_INCLUDE_DIRECTORIES}'")
#message("${MESSAGE_MODE}" "CMAKE_CXX_IMPLICIT_LINK_DIRECTORIES: '${CMAKE_CXX_IMPLICIT_LINK_DIRECTORIES}'")
#message("${MESSAGE_MODE}" "CMAKE_CXX_IMPLICIT_LINK_LIBRARIES: '${CMAKE_CXX_IMPLICIT_LINK_LIBRARIES}'")

if("$ENV{CLION_IDE}")
    message("CLION_IDE: '$ENV{CLION_IDE}'")
endif()

add_subdirectory("${PROJECT_NAME}-cpp-test")
#add_subdirectory("${PROJECT_NAME}-conan-test")
#add_subdirectory("${PROJECT_NAME}-extension-test")
#add_subdirectory("${PROJECT_NAME}-test")

set_targets("${PROJECT_NAME}_TARGETS")

add_custom_target("targets"
    COMMAND "${CMAKE_COMMAND}" -E echo ${${PROJECT_NAME}_TARGETS}
    VERBATIM
)
